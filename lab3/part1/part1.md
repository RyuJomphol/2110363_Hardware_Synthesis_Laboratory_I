# Lab 3 Part 1 - ข้อผิดพลาดและแนวทางแก้ไข HDL

## ภาพรวม
ไดเรกทอรีนี้มี 4 ไฟล์ปัญหา ที่แสดงให้เห็นข้อผิดพลาดทั่วไปในการออกแบบ HDL (Hardware Description Language) แต่ละปัญหาเน้นไปที่ปัญหาเฉพาะที่สามารถเกิดขึ้นในโครงการ FPGA/ฮาร์ดแวร์

---

## ปัญหาที่ 1: ไดรเวอร์หลายตัวในเอาต์พุตเดียว (part_1_problem_1.v)

### คำอธิบายข้อผิดพลาด
```verilog
assign y = a;  // ไดรเวอร์ตัวแรก
assign y = b;  // ไดรเวอร์ตัวที่สอง - ขัดแย้ง!
```

### ปัญหาคืออะไร
- **ประเภทข้อผิดพลาด**: ไดรเวอร์ต่อเนื่องหลายตัว (ขัดแย้งโครงสร้าง)
- **อะไรผิด**: สัญญาณเอาต์พุต `y` มีไดรเวอร์สองตัวเชื่อมต่ออยู่พร้อมกัน
- **ทำไมจึงแย่**:
  - ในฮาร์ดแวร์ คุณไม่สามารถมีแหล่งที่มาสองแหล่งขับสัญญาณเดียวกันได้พร้อมกัน
  - สิ่งนี้สร้างการแข่งขันบัส - พฤติกรรมที่ไม่สามารถคาดเดาได้
  - เครื่องสังเคราะห์จะสร้างข้อผิดพลาดหรือคำเตือน
  - ในการจำลอง ค่าจะกลายเป็นค่าที่ไม่ชัดเจน/ความต้านทานสูง (X)

### วิธีแก้ไข
ใช้ multiplexer เพื่อเลือกว่าอินพุตใดขับเอาต์พุต:
```verilog
assign y = enable ? a : b;  // ไดรเวอร์เดียวตามเงื่อนไข
```

หรือหากอินพุตทั้งสองต้องส่งผลต่อเอาต์พุต ให้ใช้การดำเนินการลอจิกที่เหมาะสม:
```verilog
assign y = a & b;  // การดำเนินการ AND
assign y = a | b;  // การดำเนินการ OR
assign y = a ^ b;  // การดำเนินการ XOR
```

### สิ่งที่ต้องเรียนรู้
- สัญญาณในลอจิกชุดสามารถถูกขับโดยแหล่งที่มาเพียงแหล่งเดียวเท่านั้น
- ใช้ multiplexers (ตัวดำเนินการ `?:`) เพื่อเลือกระหว่างไดรเวอร์หลายตัว
- ตรวจสอบข้อขัดแย้งที่ไม่ตั้งใจในการกำหนดสัญญาณเสมอ

---

## ปัญหาที่ 2: ไม่มีคำจำกัดความของโมดูล (part_1_problem_2.v)

### คำอธิบายข้อผิดพลาด
```verilog
missing_module missing_inst (
    .clk(clk),
    .enable(enable),
    .counter(counter)
);
```

### ปัญหาคืออะไร
- **ประเภทข้อผิดพลาด**: การอ้างอิงโมดูลที่ไม่ได้กำหนด
- **อะไรผิด**: โมดูล `missing_module` ถูกสร้างอินสแตนซ์ แต่ไม่มีการกำหนดไว้ที่ใดเลย
- **ทำไมจึงแย่**:
  - เครื่องมือจำลองและสังเคราะห์ไม่สามารถหาคำจำกัดความของโมดูลได้
  - ไม่สามารถคอมไพล์/อีลาบอเรตการออกแบบได้
  - ข้อความข้อผิดพลาด: `ERROR: Missing module definition for 'missing_module'`

### วิธีแก้ไข
สร้างคำจำกัดความของโมดูลที่หายไป ตัวอย่างสำหรับตัวนับ 4 บิต:
```verilog
module missing_module (
    input wire clk,
    input wire enable,
    output wire [3:0] counter
);
    reg [3:0] count = 0;
    
    always @(posedge clk) begin
        if (enable) begin
            count <= count + 1;
        end
    end
    
    assign counter = count;
endmodule
```

หรือแทนที่การสร้างอินสแตนซ์ด้วยการใช้งานจริงหากเป็นเพียงโมดูลเดียว

### สิ่งที่ต้องเรียนรู้
- กำหนดทุกโมดูลที่คุณสร้างอินสแตนซ์เสมอ
- ตรวจสอบให้แน่ใจว่าคำจำกัดความของโมดูลอยู่ในโครงการ/โฟลเดอร์เดียวกัน (หรือระบุในลำดับการคอมไพล์)
- ใช้การออกแบบแบบลำดับชั้น: โมดูลระดับบนสุดสร้างอินสแตนซ์โมดูลระดับล่าง
- ตรวจสอบบันทึก Vivado/ModelSim สำหรับข้อผิดพลาด "module not found"

---

## ปัญหาที่ 3: ข้อผิดพลาดในไฟล์ข้อจำกัด (part_1_problem_3.v)

### คำอธิบายข้อผิดพลาด
```verilog
module part_1_problem_3 (
    input  wire a,
    input  wire b,
    output wire led  // สัญญาณลอจิก ต้องการการกำหนดพินฟิสิคัล
);
```

### ปัญหาคืออะไร
- **ประเภทข้อผิดพลาด**: ข้อจำกัดพินหายไป
- **อะไรผิด**: พอร์ต `led` ไม่ได้ถูกกำหนดให้กับพินฟิสิคัลบนบอร์ด FPGA
- **ทำไมจึงแย่**:
  - เครื่องสังเคราะห์ไม่สามารถวางตรรกะบนฮาร์ดแวร์จริงได้
  - ไม่มีการเชื่อมต่อฟิสิคัลระหว่างลอจิกและ LED
  - ข้อผิดพลาดระหว่างการใช้งาน: `ERROR: Missing constraint for port 'led'`
  - เฉพาะบอร์ด: ไฟล์ข้อจำกัด (.xdc) หายไปการกำหนดพิน

### วิธีแก้ไข
สร้างหรืออัปเดตไฟล์ `.xdc` (Xilinx Design Constraint) ด้วยการกำหนดพิน

ตัวอย่าง [part_1_problem_3.xdc](part_1_problem_3.xdc):
```xdc
# การกำหนดพิน LED สำหรับบอร์ด Basys3
set_property PACKAGE_PIN H17 [get_ports led]
set_property IOSTANDARD LVCMOS33 [get_ports led]

# การกำหนดพินอินพุต
set_property PACKAGE_PIN N17 [get_ports a]
set_property IOSTANDARD LVCMOS33 [get_ports a]

set_property PACKAGE_PIN P18 [get_ports b]
set_property IOSTANDARD LVCMOS33 [get_ports b]
```

### สิ่งที่ต้องเรียนรู้
- **ไฟล์ข้อจำกัด** กำหนด:
  - การกำหนดพินฟิสิคัล (PACKAGE_PIN)
  - มาตรฐานแรงดันไฟ I/O (IOSTANDARD)
  - ข้อจำกัดเวลา
  - ข้อจำกัดพลังงาน
- สำหรับบอร์ด FPGA ให้ตรวจสอบคู่มืออ้างอิงของบอร์ดเพื่อตำแหน่งพิน
- ทุกพอร์ต I/O ต้องมีข้อจำกัด PACKAGE_PIN อย่างน้อย
- บอร์ดต่างกัน (Basys3, Arty, Nexys4 เป็นต้น) มีการกำหนดพินที่แตกต่างกัน

---

## ปัญหาที่ 4: ข้อผิดพลาดการใช้ทรัพยากรเกิน (part_1_problem_4.v)

### คำอธิบายข้อผิดพลาด
```verilog
reg [7:0] memory_array [0:1048575];  // หน่วยความจำ 1M x 8-bit
```

### 🔴 ปัญหาคืออะไร
- **ประเภทข้อผิดพลาด**: การใช้ทรัพยากรมากเกินไป / การล้นของการออกแบบ
- **อะไรผิด**: พยายามจัดสรร 1M × 8-bit = **หน่วยความจำ 1 Gigabyte**
- **ทำไมจึงแย่**:
  - บอร์ด FPGA มีหน่วยความจำจำกัดมาก (โดยปกติ 4-36 MB)
  - การออกแบบนี้ต้องการหน่วยความจำมากกว่าที่มีอยู่หลายลำดับความสำคัญ
  - การสังเคราะห์จะล้มเหลวหรือสร้างคำเตือน
  - ข้อผิดพลาด: `ERROR: Cannot allocate 1GB of resources on FPGA with limited LUTs/Block RAM`
  - การใช้พลังงานและการสร้างความร้อนจากคำสูง

### การเปรียบเทียบหน่วยความจำ
| ประเภทหน่วยความจำ | พร้อมใช้บน Basys3 |
|------------|---------------------|
| Block RAM | 36 KB (36,864 bytes) |
| LUT RAM | ~50 KB (ประมาณการ) |
| **การออกแบบต้องการ** | **1,048,576 KB** ❌ |

### วิธีแก้ไข

**ตัวเลือกที่ 1: ใช้เพียงหน่วยความจำตามที่ต้องการ**
```verilog
// สำหรับ 256 ไบต์ (เล็กกว่ามาก)
reg [7:0] memory_array [0:255];
```

**ตัวเลือกที่ 2: ใช้ FPGA Block RAM อย่างมีประสิทธิภาพ**
```verilog
// สำหรับ 4KB ใช้ Block RAM
reg [7:0] memory_array [0:4095];

always @(posedge clk) begin
    if (write_enable) begin
        memory_array[address[11:0]] <= din;  // ใช้ 12 บิตของที่อยู่เท่านั้น
    end else begin
        dout_reg <= memory_array[address[11:0]];
    end
end
```

**ตัวเลือกที่ 3: ใช้หน่วยความจำภายนอก**
หากคุณต้องการหน่วยความจำขนาดใหญ่จริงๆ:
- ใช้ SDRAM ผ่านอินเทอร์เฟส SPI
- เพิ่มโมดูลหน่วยความจำภายนอก
- ข้อมูลบัฟเฟอร์ผ่าน FPGA

### สิ่งที่ต้องเรียนรู้
- รู้เกี่ยวกับทรัพยากรของบอร์ด FPGA:
  - LUTs (ตารางการค้นหา)
  - Block RAM (BRAM)
  - หน่วยความจำทั้งหมดที่มีอยู่
- คำนวณความต้องการหน่วยความจำ: `ความลึก × ความกว้าง (เป็นไบต์)`
- Basys3 มี Block RAM เพียง 36KB - ใช้อย่างชาญฉลาด!
- ใช้รายงานทรัพยากรของเครื่องมือสังเคราะห์เพื่อติดตามการใช้งาน
- ใช้กลยุทธ์การจัดการหน่วยความจำที่มีประสิทธิภาพ

---

## ตารางสรุป

| ปัญหา | ประเภทข้อผิดพลาด | สาเหตุ | วิธีแก้ไข |
|---------|-----------|-------|----------|
| ปัญหาที่ 1 | ไดรเวอร์หลายตัว | สองคำสั่ง `assign` ในสัญญาณเดียวกัน | ใช้ multiplexer หรือการกำหนดเดียว |
| ปัญหาที่ 2 | โมดูลหายไป | โมดูลที่อ้างอิงแต่ไม่ได้กำหนด | สร้าง/กำหนดโมดูลที่หายไป |
| ปัญหาที่ 3 | ข้อจำกัดหายไป | ไม่มีการกำหนดพินสำหรับ I/O | เพิ่มการกำหนดพินในไฟล์ .xdc |
| ปัญหาที่ 4 | ใช้ทรัพยากรเกิน | ขอหน่วยความจำมากเกินไป | ลดขนาดหน่วยความจำหรือใช้หน่วยความจำภายนอก |

---

## วิธีการแก้จุดบกพร่องของข้อผิดพลาดเหล่านี้

### ใน Vivado:
1. **ขั้นตอน Elaboration**: จับข้อผิดพลาด 1, 2 และ 4
2. **ขั้นตอน Synthesis**: รายงานการใช้ทรัพยากรและข้อขัดแย้ง
3. **ขั้นตอน Implementation**: จับข้อผิดพลาดข้อจำกัด (ปัญหาที่ 3)
4. ตรวจสอบ **รายงาน Synthesis/Implementation** สำหรับข้อความข้อผิดพลาดโดยละเอียด

### ใน ModelSim/Simulation:
1. ข้อผิดพลาด 1 และ 2 ปรากฏในระหว่างการอีลาบอเรต
2. ข้อผิดพลาด 3 ไม่ส่งผลต่อการจำลอง (ไม่จำเป็นต้องมีพินฟิสิคัล)
3. ข้อผิดพลาด 4 ทำให้การจำลองค้างหรือช้าลงอย่างมาก

---

## คำสั่งทั่วไปเพื่อตรวจสอบทรัพยากร

**ในคอนโซล Vivado TCL:**
```tcl
report_utilization
report_design_analysis
check_syntax
```

**ใน Makefile/Terminal:**
```bash
vivado -mode batch -source run_synthesis.tcl
```

---

## อ้างอิง
- [เอกสาร Xilinx Constraints Files (.xdc)](https://docs.xilinx.com/r/en-US/ug912-vivado-properties)
- [คู่มืออ้างอิงบอร์ด Basys3](https://reference.digilentinc.com/reference/programmable-logic/basys-3/reference-manual)
- แนวปฏิบัติที่ดีที่สุด Verilog HDL
- คู่มือการวางแผนทรัพยากร FPGA