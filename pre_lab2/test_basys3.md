# Basys3 Test Guide (Vivado)

เอกสารนี้เป็นไกด์สำหรับให้น้อง ๆ ใช้ทดสอบบอร์ด **Basys3** เบื้องต้น โดยใช้โปรแกรม **Vivado** และตัวอย่างวงจรง่าย ๆ (LED กระพริบ)

> 📌 แหล่งอ้างอิงหลัก

* Digilent: Getting Started with Vivado
  [https://digilent.com/reference/programmable-logic/guides/getting-started-with-vivado](https://digilent.com/reference/programmable-logic/guides/getting-started-with-vivado)

---

## 1. Create a Vivado Project

* เปิดโปรแกรม **Vivado**
* เลือก **Create Project**
* ตั้งชื่อโปรเจกต์ และเลือกที่จัดเก็บไฟล์
* เลือกชนิดโปรเจกต์เป็น **RTL Project**

ขั้นตอนนี้เป็นการสร้างโครงสร้างโปรเจกต์พื้นฐานสำหรับใช้งาน Vivado

---

## 2. The Project Manager

หลังจากสร้างโปรเจกต์แล้ว จะเข้าสู่หน้า **Project Manager** ซึ่งใช้สำหรับ

* เพิ่มไฟล์ Verilog
* เพิ่ม Constraint File
* Run Synthesis / Implementation
* Generate Bitstream

ในไกด์นี้เราจะใช้เมนูเหล่านี้เป็นหลัก

---

## 3. Adding a Constraint File

Constraint File (`.xdc`) ใช้สำหรับกำหนดว่า **สัญญาณในโค้ด Verilog จะไปเชื่อมกับขาใดบนบอร์ดจริง**

ให้สร้างไฟล์ `.xdc` และใส่โค้ดดังนี้

```tcl
set_property -dict {PACKAGE_PIN W5 IOSTANDARD LVCMOS33} [get_ports clk]
create_clock -add -name sys_clk_pin -period 10.00 -waveform {0 5} [get_ports clk]

set_property -dict {PACKAGE_PIN U16 IOSTANDARD LVCMOS33} [get_ports {led}]
```

### อธิบายโค้ด Constraint

* `set_property -dict {PACKAGE_PIN W5 ...} [get_ports clk]`

  * กำหนดให้สัญญาณ `clk` ใน Verilog เชื่อมกับขา **W5** บนบอร์ด Basys3
  * ขา W5 คือ **Clock 100 MHz** บนบอร์ด

* `IOSTANDARD LVCMOS33`

  * กำหนดมาตรฐานแรงดันไฟฟ้าเป็น **3.3V (LVCMOS33)**

* `create_clock -period 10.00`

  * กำหนด Clock Constraint
  * period = 10 ns → ความถี่ **100 MHz**

* `set_property -dict {PACKAGE_PIN U16 ...} [get_ports {led}]`

  * กำหนดให้สัญญาณ `led` เชื่อมกับขา **U16** ซึ่งเป็น LED บนบอร์ด Basys3

---

## 4. Creating a Verilog Source File

สร้างไฟล์ Verilog และใส่โค้ดดังนี้

```verilog
module blinky (
    input clk,
    output led
);

reg [24:0] count = 0;

assign led = count[24];

always @(posedge clk)
    count <= count + 1;

endmodule
```

### อธิบายโค้ด Verilog

* `module blinky`

  * เป็นโมดูลหลักของโปรเจกต์ ใช้สำหรับทดสอบ LED

* `input clk`

  * รับสัญญาณ Clock จากบอร์ด (100 MHz)

* `output led`

  * ส่งสัญญาณไปควบคุม LED บนบอร์ด

* `reg [24:0] count`

  * ตัวแปรนับค่า (Counter) ขนาด 25 บิต
  * ใช้เพื่อลดความเร็วของ Clock ให้มองเห็นการกระพริบของ LED

* `always @(posedge clk)`

  * ทำงานทุกครั้งที่ Clock เปลี่ยนจาก 0 → 1 (Rising Edge)

* `count <= count + 1;`

  * เพิ่มค่าตัวนับทีละ 1 ทุก Clock

* `assign led = count[24];`

  * นำบิตที่ 24 ของตัวนับไปควบคุม LED
  * ทำให้ LED กระพริบช้าลงจนสามารถมองเห็นได้

---

## 5. Generate a Bitstream

* กด **Run Synthesis**
* กด **Run Implementation**
* เลือก **Generate Bitstream**

ขั้นตอนนี้เป็นการแปลงโค้ด Verilog ให้เป็นไฟล์ที่ FPGA เข้าใจได้

---

## 6. Programming an FPGA Board through Vivado's Hardware Manager

* ต่อบอร์ด **Basys3** กับคอมพิวเตอร์ผ่าน USB
* เปิด **Hardware Manager**
* เลือก **Open Target → Auto Connect**
* เลือกไฟล์ Bitstream และกด **Program Device**

หากทุกอย่างถูกต้อง LED บนบอร์ดจะเริ่มกระพริบทันที 🎉

---
